// Parameter and Return

var nambah = (number1, number2) => {
  return number1 + number2;
};

var result = nambah(3, 9.1);
console.log(result);

var fullName = (first, last) => {
  return `${first} ${last}`;
};

var result = fullName("Alfath", "Sien");
console.log(result);
